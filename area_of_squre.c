#include <stdio.h>
#include <conio.h>

int main()
{
    int side, area;
    printf("Enter the side=");
    scanf("%d", &side);
    area = side * side;
    printf("area of squre is:%d", area);
    return 0;
}
#include <stdio.h>
#include <conio.h>

int main()
{
    int a, b, divide;
    printf("Enter the number a=");
    scanf("%d", &a);
    printf("Enter the number b=");
    scanf("%d", &b);
    divide = a / b;
    printf("divide is:%d", divide);
    return 0;
}
